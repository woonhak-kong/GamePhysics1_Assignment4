using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MyUtil : MonoBehaviour
{
   static public float GetAngleBetweenVectors(Vector3 start, Vector3 end)
    {

        float dotResult = Vector3.Dot(start, end);

        return 0;
    }
}
